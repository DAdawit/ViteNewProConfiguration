import React from 'react';

const InReview = ({ text }) => {
  return (
    <h4 className="text-primary-900 bg-primary-50 rounded-[20px] px-2 py-1">
      {text}
    </h4>
  );
};

export default InReview;
