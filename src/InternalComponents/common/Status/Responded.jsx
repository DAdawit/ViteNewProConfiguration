import React from "react";

const Responded = ({ text }) => {
  return (
    <h4 className="text-primary-900 bg-primary-50 rounded-[20px] px-2 py-1 w-max text-center whitspace-nowrap text-sm">
      {text}
    </h4>
  );
};

export default Responded;
