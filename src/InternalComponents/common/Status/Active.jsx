import React from "react";

const Active = ({ text }) => {
  return (
    <h4 className="text-success-900 text-sm px-3 py-1 bg-success-50 rounded-full border-0 w-fit">
      {text}
    </h4>
  );
};

export default Active;
