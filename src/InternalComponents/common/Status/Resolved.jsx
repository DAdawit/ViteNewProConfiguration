import React from "react";

const Responded = ({ text }) => {
  return (
    <h4 className="text-success-900 bg-success-50 rounded-[20px] px-2 py-1 w-max whitespace-nowrap ">
      {text}
    </h4>
  );
};

export default Responded;
